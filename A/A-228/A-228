#include <bits/stdc++.h>
using namespace std;
int main()
{
    short int i,sum=0,n=4;
    int tab[n];
    for(i=0;i<n;i++){
        cin >> tab[i];
    }
    sort(tab,tab+n);
    for(i=0;i<n-1;i++){
        if(tab[i]==tab[i+1])
            sum++;
    }
    cout << sum;
}
