#include<bits/stdc++.h>
using namespace std; 
int main(){
	short int n,i;
	cin >> n;
	int tab[n];
	for(i=0;i<n;i++){
		cin >> tab[i];
	}
	sort(tab,tab+n);
	n%2==0?cout << tab[n/2-1]:cout<<tab[(n-1)/2]<<endl;
}
