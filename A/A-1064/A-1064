#include<bits/stdc++.h>
using namespace std;
int main(){
	short int a,b,c,x=0;
	cin >> a >> b >> c;
	x = a+b+c-max(a,max(b,c));
	if(x>max(a,max(b,c)))
		cout << 0;
	else
		cout << max(a,max(b,c)) - x +1;
}
