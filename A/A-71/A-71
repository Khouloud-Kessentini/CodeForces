#include<bits/stdc++.h>
using namespace std;
int main(){
	short int t;
	cin >> t ;
	char s[t][100];
	while(t--){
		cin >> s[t];
		if(strlen(s[t])>10){
			cout<< s[t][0] << (strlen(s[t])-2)<<s[t][strlen(s[t])-1]<<endl;
		}
		else
			cout << s[t]<< endl;
	}
}
