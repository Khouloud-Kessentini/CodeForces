#include <bits/stdc++.h>
using namespace std;
int main()
{
    short int x1,x2,x3,p1,p2=0,p3;
    cin >> x1 >> x2 >> x3;
    short int sum=x1+x2+x3;
    p1 = max(x1,max(x2,x3)); 
    p3 = min(x1,min(x2,x3)); 
    p2 = sum -p1 - p3;      
    cout << p1-p2+p2-p3;
}
