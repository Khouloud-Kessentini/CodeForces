#include<bits/stdc++.h>
using namespace std;
int main(){
	short int n, i;
	int t;
	cin >> t;
	while(t--){
		cin >> n;
		int tab[n];
		for(i=0; i<n; i++){
			cin >> tab[i];
		}
		sort(tab, tab+n);
		cout << tab[n-1] - tab[0] << endl;
	}
}
