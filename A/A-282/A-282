#include <bits/stdc++.h>
using namespace std;
int main()
{
    short int n,sum=0,x=3;
    cin >> n;
    char p[n][x];
    while(n--){
        cin >> p[n];
        p[n][1]=='+'?sum++:sum--;
    }
    cout << sum;
}
